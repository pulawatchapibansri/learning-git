# 📚 Git & GitHub Cheat Sheet (คัมภีร์ฉบับผู้เริ่มต้น)

สรุปคำสั่งทั้งหมดที่คุณริวได้เรียนรู้และลงมือทำจริงครับ!

## 1. การตั้งค่าครั้งแรก (ทำแค่ครั้งเดียวในคอมพิวเตอร์)
- `git config --global user.name "ชื่อของคุณ"` : แนะนำตัว (ชื่อ)
- `git config --global user.email "อีเมลของคุณ"` : แนะนำตัว (อีเมลต้องตรงกับ GitHub)

## 2. เริ่มต้นโปรเจกต์
- `git clone <URL>` : ดาวน์โหลด (คัดลอก) โปรเจกต์จาก GitHub ลงมาที่เครื่องคอมพิวเตอร์ของเรา

## 3. วงจรชีวิตประจำวัน (Daily Workflow)
คอมโบ 4 คำสั่งที่โปรแกรมเมอร์ต้องใช้ทุกวันหลังจากแก้ไข/เซฟไฟล์เสร็จ:
1. `git status` : ตรวจสอบสถานะ (สีแดง = ยังไม่ถูกบันทึก, สีเขียว = พร้อมบันทึก)
2. `git add .` : **หยิบใส่กล่อง** (แพ็กไฟล์ทุกอย่างที่มีการแก้ไขเข้า Staging Area)
3. `git commit -m "ข้อความอธิบาย"` : **ปิดผนึกกล่อง** (บันทึกประวัติพร้อมเขียนป้ายแปะว่าทำอะไรไปบ้าง)
4. `git push` : **ส่งของ** (อัปโหลดประวัติทั้งหมดขึ้นไปเก็บบนเซิร์ฟเวอร์ GitHub)

## 4. การดึงข้อมูลอัปเดต (Pull)
- `git pull` : ดึงข้อมูลล่าสุดจาก GitHub ลงมาอัปเดตในเครื่องเรา (ใช้บ่อยตอนทำงานเป็นทีม หรือเราเผลอแก้ไฟล์บนเว็บ)

## 5. การจัดการจักรวาลคู่ขนาน (Branching)
- `git branch` : ดูรายชื่อกิ่งทั้งหมดในเครื่อง (กิ่งที่ยืนอยู่จะมีตัว `*` สีเขียว)
- `git branch -a` : ดูรายชื่อกิ่งทั้งหมด (ทั้งในคอมฯ และบนอินเทอร์เน็ต)
- `git checkout -b <ชื่อกิ่ง>` : **สร้างกิ่งใหม่** พร้อมกับกระโดดไปที่กิ่งนั้นเลยทันที
- `git checkout <ชื่อกิ่ง>` : กระโดดสลับไปมาระหว่างกิ่ง (เช่น `git checkout main`)
- `git push -u origin <ชื่อกิ่ง>` : ดันกิ่งลับของเราขึ้นไปสร้างบนเน็ตให้โลกรับรู้

## 6. การรวมร่าง (Merging)
เมื่อทำงานในกิ่งทดลองเสร็จแล้ว และต้องการดึงเนื้องานมารวมเข้าลำต้นหลัก:
1. `git checkout main` : วาร์ปกลับมายืนรอที่กิ่งหลักก่อน
2. `git merge <ชื่อกิ่งทดลอง>` : สั่งดูดเนื้องานจากกิ่งทดลองมารวมเข้ากับ `main` ทันที
3. `git push` : อย่าลืมส่ง `main` ที่อัปเกรดแล้วขึ้นเว็บด้วย!

---
**🚨 กฎเหล็กของโปรแกรมเมอร์:** 
1. **ห้ามเก็บโปรเจกต์ Git ไว้ในระบบ Cloud (Google Drive, OneDrive, Dropbox) เด็ดขาด!** ให้เก็บไว้ในฮาร์ดดิสก์ธรรมดา เช่น ไดรฟ์ D: เพื่อป้องกันไฟล์ประวัติพัง
2. **ต้องกด Save (Ctrl+S) ทุกครั้ง!** ก่อนที่จะเริ่มพิมพ์คำสั่ง `git add`
